services:
  nginx:
    image: nginx:latest
    container_name: nginx

    ports:
      - "4000:80"

    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s

    restart: unless-stopped
